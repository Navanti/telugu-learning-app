{
  "name": "@telugu/desktop",
  "version": "0.2.0",
  "private": true,
  "description": "Desktop distribution for Telugu Learning App",
  "main": "dist/main.js",
  "scripts": {
    "dev": "concurrently -k \"pnpm --filter @telugu/web dev\" \"wait-on tcp:3000 && electron .\"",
    "build:web": "pnpm --filter @telugu/web build",
    "build": "tsc -p tsconfig.json",
    "dist": "pnpm run build:web && pnpm run build && electron-builder --publish never",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "dist:win": "pnpm run build:web && pnpm run build && electron-builder --win nsis --x64 --publish never",
    "dist:mac": "pnpm run build:web && pnpm run build && electron-builder --mac dmg --x64 --arm64 --publish never",
    "dist:linux": "pnpm run build:web && pnpm run build && electron-builder --linux AppImage --x64 --publish never"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "electron": "^35.0.0",
    "electron-builder": "^26.0.12",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "wait-on": "^8.0.3"
  },
  "build": {
    "appId": "com.telugu.learning.app",
    "productName": "Telugu Learning App",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "../web/out",
        "to": "web"
      }
    ],
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    },
    "mac": {
      "category": "public.app-category.education",
      "target": [
        {
          "target": "dmg",
          "arch": [
            "x64",
            "arm64"
          ]
        }
      ]
    },
    "dmg": {
      "artifactName": "${productName}-${version}-${arch}.${ext}"
    },
    "linux": {
      "category": "Education",
      "target": [
        {
          "target": "AppImage",
          "arch": [
            "x64"
          ]
        }
      ]
    }
  }
}
